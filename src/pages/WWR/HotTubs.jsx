import React from "react";
import { Box, Typography } from "@mui/material";

const HotTubs = () => (
  <Box sx={{ p: 4, textAlign: "center" }}>
    <Typography variant="h3" sx={{ fontWeight: 700 }}>
      Hot Tubs Removal
    </Typography>
    <Typography sx={{ mt: 2 }}>
      Details about hot tubs removal go here.
    </Typography>
  </Box>
);

export default HotTubs;
