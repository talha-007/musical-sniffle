import React from "react";
import { Box, Typography } from "@mui/material";

const Pallets = () => (
  <Box sx={{ p: 4, textAlign: "center" }}>
    <Typography variant="h3" sx={{ fontWeight: 700 }}>
      Pallets Removal
    </Typography>
    <Typography sx={{ mt: 2 }}>
      Details about pallets removal go here.
    </Typography>
  </Box>
);

export default Pallets;
